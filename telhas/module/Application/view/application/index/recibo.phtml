<style>
    .right {
        text-align: right;
    }
</style>

<?php for ($i = 0; $i < 2; $i++): ?>

    <table class="tg">
        <tr>
            <td class="tg-5wxp">
                <img src="<?= $logo ?>" width="100" height="80">
            </td>
            <td class="tg-9wxp" colspan="2"><strong>CORABRAS</strong></td>
        </tr>

        <tr>
            <td colspan="3"><br><br></td>
        </tr>

        <tr>
            <td class="tg-9yu8" colspan="3"><strong>RECIBO</strong></td>
        </tr>

        <tr>
            <td colspan="3"><br><br></td>
        </tr>

        <tr>
            <td colspan="3">
                Recebemos de <?= $pedido['nome'] ?> a quantia supra algarismada de
                <strong>R$ <?= $valor_total_g ?> (<?= $valor_extenso ?>)</strong>
                referente ao pagamento de telhas de concreto.
            </td>
        </tr>

        <tr>
            <td colspan="3"><br><br></td>
        </tr>
        <?php
        $dataPorExtenso = '';
        if (class_exists('\IntlDateFormatter')) {
            $fmt = new \IntlDateFormatter(
                'pt_BR',
                \IntlDateFormatter::FULL,
                \IntlDateFormatter::NONE,
                'America/Sao_Paulo',
                \IntlDateFormatter::GREGORIAN,
                "EEEE, dd 'de' MMMM 'de' yyyy"
            );
            if ($fmt) {
                $dataPorExtenso = ucfirst($fmt->format(new \DateTime()));
            }
        }
        if (empty($dataPorExtenso)) {
            $dias = ['Domingo', 'Segunda-feira', 'Terça-feira', 'Quarta-feira', 'Quinta-feira', 'Sexta-feira', 'Sábado'];
            $meses = ['', 'janeiro', 'fevereiro', 'março', 'abril', 'maio', 'junho', 'julho', 'agosto', 'setembro', 'outubro', 'novembro', 'dezembro'];
            $w = (int)date('w');
            $d = (int)date('d');
            $m = (int)date('n');
            $y = (int)date('Y');
            $dataPorExtenso = "{$dias[$w]}, {$d} de {$meses[$m]} de {$y}";
        }
        ?>

        <tr>
            <td colspan="3" class="right"><?= $dataPorExtenso ?></td>
        </tr>

        <tr>
            <td><strong><?= $qtd_total ?> PEÇAS</strong></td>
            <td colspan="2"><br><br><br><br>______________________________________________________________</td>
        </tr>
    </table>

<?php endfor; ?>