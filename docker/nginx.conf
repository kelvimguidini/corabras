# -------------------------------------------------------------
# 1. Corabras Blocos - Porta 9010
# -------------------------------------------------------------
server {
    listen 9010;
    server_name localhost;

    root /var/www/html/blocos/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_read_timeout 300;
    }

    location ~ /\.ht {
        deny all;
    }
}

# -------------------------------------------------------------
# 2. Corabras Telhas - Porta 9011
# -------------------------------------------------------------
server {
    listen 9011;
    server_name localhost;

    root /var/www/html/telhas/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_read_timeout 300;
    }

    location ~ /\.ht {
        deny all;
    }
}

# -------------------------------------------------------------
# 3. dfTelhas - Porta 9012
# -------------------------------------------------------------
server {
    listen 9012;
    server_name localhost;

    root /var/www/html/dftelhas/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_read_timeout 300;
    }

    location ~ /\.ht {
        deny all;
    }
}
